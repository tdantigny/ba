<header class="container">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a href="{{ path('fo_home') }}">
                    <img src="{{ asset('apple-touch-icon.png') }}" alt="Bonne augure" title="Bonne augure">
                </a>
            </div>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('fo_home') }}">Accueil</a></li>
                    <li><a href="{{ path('fo_horoscope') }}">Horoscope</a></li>
                    <li><a href="#">Guide pratique</a></li>
                    <li><a href="#">Annuaire</a></li>
                    <li><a href="#">Voyants disponibles</a></li>
                    <li><a href="#">Contact</a></li>
                    <li>
                        {% if is_granted("ROLE_ADMIN") %}
                            <a href="{{ path('bo_home') }}">Back-Office</a>
                        {% endif %}
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        Connecté en tant que {{ app.user.lastName }}  {{ app.user.firstName }} |
                        <a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
                    {% else %}
                        <li><a href="{{ path('fo_login') }}">Login</a></li>
                        <li><a href="{{ path('fo_register') }}">Inscription</a></li>
                    {% endif %}

                </ul>
            </div>
        </div>
    </nav>
</header>
{% extends '::base.html.twig' %}

{% block body %}

    Détail

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="Modifier" />
        </div>
    {{ form_end(form) }}

    <button id="enabledUser" onclick="disableUser({{ user.id }})">{% if user.enabled %}Désactiver{% else %}Activer{% endif %}</button>
    <button id="enabledUser" onclick="depersonalizeUser({{ user.id }})">Dépersonnaliser</button>
{% endblock %}
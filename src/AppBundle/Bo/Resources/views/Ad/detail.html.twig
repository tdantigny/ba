{% extends '::base.html.twig' %}

{% block body %}
    <h2>Modifier une pub</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="Modifier" />
    </div>
    {{ form_end(form) }}
{% endblock %}
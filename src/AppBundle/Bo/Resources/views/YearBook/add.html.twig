{% extends '::base.html.twig' %}

{% block body %}
    {{ tinymce_init() }}
    <h2>Créer un annuaire</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="Créer" />
        </div>
    {{ form_end(form) }}

    <a href="{{ path('bo_year_book_list') }}">Annuler</a>
{% endblock %}